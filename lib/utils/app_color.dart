import 'package:flutter/material.dart';

final ButtonStyle buttonPrimary = ElevatedButton.styleFrom(
  
  minimumSize: const Size(300, 50), backgroundColor: const Color(0xffFFFFFF),
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(50))
  )
);
final ButtonStyle buttonCardPrimary = ElevatedButton.styleFrom(

    minimumSize: const Size(200, 50), backgroundColor: const Color(0xffFFFFFF),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(50))
    )
);

final ButtonStyle buttonPrimarytwo = ElevatedButton.styleFrom(

    minimumSize: const Size(300, 50), backgroundColor: const Color(0xff555E75),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(50))
    )
);

final ButtonStyle buttonCardtwo = ElevatedButton.styleFrom(

    minimumSize: const Size(220, 40), backgroundColor: const Color(0xff555E75),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(50))
    )
);

final ButtonStyle buttonPrimarythree = ElevatedButton.styleFrom(

minimumSize: const Size(300, 60), backgroundColor: const Color(0xff555E75),
elevation: 0,
shape: const RoundedRectangleBorder(
borderRadius: BorderRadius.all(Radius.circular(0))
)
);
ButtonStyle buttonPrimarythreee = ElevatedButton.styleFrom(

    minimumSize: const Size(300, 60), backgroundColor: const Color(0xffEDEDED),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(0))
    )
);
final ButtonStyle buttonSceondary = ElevatedButton.styleFrom(

    minimumSize: const Size(300, 50), backgroundColor: const Color(0xff555E75),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(50))
    )
);
final ButtonStyle buttongive  = ElevatedButton.styleFrom(
    minimumSize: const Size(20, 40), backgroundColor: const Color(0xff898c8f),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(30),

        )
    )
);

final ButtonStyle buttonthird = ElevatedButton.styleFrom(

    minimumSize: const Size(120, 80), backgroundColor: const Color(0xffffffff),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(50))
    )
);

final ButtonStyle buttonfour = ElevatedButton.styleFrom(

    minimumSize: const Size(90, 20), backgroundColor: const Color(0xffDDDDDD),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(20))
    )
);

final ButtonStyle buttonfive = ElevatedButton.styleFrom(

    minimumSize: const Size(90, 20), backgroundColor: const Color(0xffffffff),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(20))
    )
);

final ButtonStyle buttonSix = ElevatedButton.styleFrom(

    minimumSize: const Size(300, 50), backgroundColor: const Color(0xffF2C6BD),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(0))
    )
);
final ButtonStyle buttonSeven = ElevatedButton.styleFrom(

    minimumSize: const Size(300, 50), backgroundColor: const Color(0xffEDEDED),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(0))
    )
);
final ButtonStyle buttonEight = ElevatedButton.styleFrom(

    minimumSize: const Size(300, 50), backgroundColor: const Color(0xff555E75),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(0))
    )
);
final ButtonStyle storeBtnOne = ElevatedButton.styleFrom(

    minimumSize: const Size(70, 20), backgroundColor: const Color(0xff555E75),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(20))
    )
);
final ButtonStyle storeBtnTwo = ElevatedButton.styleFrom(

    minimumSize: const Size(100, 70), backgroundColor: const Color(0xff555E75),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(20))
    )
);



final ButtonStyle signupBtnOne = ElevatedButton.styleFrom(

    minimumSize: const Size(600, 50), backgroundColor: const Color(0xffF9E000),
    padding: const EdgeInsets.all(4),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(0))
    )
);

final ButtonStyle signupBtntwo= ElevatedButton.styleFrom(

    minimumSize: const Size(600, 50), backgroundColor: const Color(0xfff3f2f2),
    padding: const EdgeInsets.all(4),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(0))
    )
);



